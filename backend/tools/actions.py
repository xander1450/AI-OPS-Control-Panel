def disable_vendor_ABC():
    return "Vendor ABC disabled"

def enable_fallback_vendor():
    return "Fallback vendor enabled"

ACTIONS = {
    "disable_vendor_ABC": disable_vendor_ABC,
    "enable_fallback_vendor": enable_fallback_vendor
}
